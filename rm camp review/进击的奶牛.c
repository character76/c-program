#include<stdio.h>
#include<stdlib.h>
int loc[10000]={0};
int main()
{
    int n,f;
    scanf("%d %d",&n,&f);
    for(int i=0;i<n;i++)
    {
        scanf("%d",loc[i]);
    }
    check();
    return 0;
}
int check(int *arr,int len)
{
    
}
int compare_func(const void* a,const void* b)
{
    
}